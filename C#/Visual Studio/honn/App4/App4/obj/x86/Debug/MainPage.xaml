<Page
    x:Class="App4.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App4"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid Background="#FEEEF2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                Margin="10,5"
                FontSize="18"
                TextTrimming="WordEllipsis"
                Text="{Binding Path=DocumentTitle,
                    ElementName=WebView1,Mode=OneWay}" />

            <WebView x:ConnectionId='2' Grid.Row="1"
                x:Name="WebView1"
                Source="http://msdn.microsoft.com/ja-JP/windows/apps/" />

            <Grid Grid.Row="2" Background="#FF020255">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <AppBarButton x:ConnectionId='7' x:Name="BackButton"
                                  Icon="Back"
                                  IsCompact="true"
                                  Margin="0,0,10,0"
                                  IsEnabled="{Binding Path=CanGoBack,
                                        ElementName=WebView1,
                                  Mode=OneWay}"                         />
                </StackPanel>

                <TextBox x:ConnectionId='3' x:Name="TextBox1" Grid.Column="1" VerticalAlignment="Center" />

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <AppBarButton x:ConnectionId='4' x:Name="GoButton"
                                  Icon="GO"
                                  IsCompact="true"                        />
                    <AppBarButton x:ConnectionId='5' x:Name="RefreshButton"
                                  Icon="Refresh"
                                  IsCompact="true"
                                  Margin="10,0,0,0"                            />
                    <AppBarButton x:ConnectionId='6' x:Name="ForwardButton"
                                  Icon="Forward"
                                  IsCompact="true"
                                  IsEnabled="{Binding Path=CanGoForward,
                                        ElementName=WebView1,
                                        Mode=OneWay}"                            />
                </StackPanel>

            </Grid>
                  
        </Grid>

    </Grid>
</Page>

